[build]
  command = "cd frontend && npm install && npm run build -- --config vue.config.js"
  publish = "frontend/dist"
  port = 3100

[[plugins]]
  package = "netlify-plugin-cache"

[build.environment]
  VUE_APP_API = "http://localhost:3100"

